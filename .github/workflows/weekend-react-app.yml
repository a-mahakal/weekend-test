name: Deploying weekend app

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        ref: "master"

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20  # Use the Node.js version required by your app

    - name: Install Dependencies
      run: npm install

    - name: Build React App
      run: npm run build

    # - name: Deploy to GitHub Pages
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.ANURAG_GITHUB_TOKEN }}
    #   run: npm run deploy
